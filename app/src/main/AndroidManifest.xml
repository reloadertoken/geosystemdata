<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.reloader.sacooliveros">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mPrincipalActivity" />
        <activity android:name=".Helico_Materiales.HelicoMaterialesActivity" />
        <activity android:name=".Helico_Materiales.Tomos.Tomo1MenuActivity" />
        <activity android:name=".Helico_Materiales.Tomos.Materias.CienciasActivity"></activity>
    </application>

</manifest>